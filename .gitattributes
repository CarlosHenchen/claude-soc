# Normalize line endings (LF) so the repo is consistent across OSes.
* text=auto eol=lf

# Binary assets — no EOL normalization / no diff noise.
*.xlsx binary
*.png binary
*.jpg binary
*.ico binary
assets/echarts.min.js binary
web/echarts.min.js binary
assets/xlsx.full.min.js binary
web/xlsx.full.min.js binary
